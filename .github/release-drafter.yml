name-template: "v$RESOLVED_VERSION"
tag-template: "v$RESOLVED_VERSION"
categories:
  # PRs with this label are left out of the release notes entirely.
  - type: "pre-exclude"
    when:
      label: "skip-release"
  # major/minor/patch labels are intentionally not used for version
  # resolution: Dependabot applies them to describe the dependency's own
  # version jump, which says nothing about this project's API.
  - type: "version-resolver"
    semver-increment: "major"
    when:
      label: "breaking"
  - type: "version-resolver"
    semver-increment: "minor"
    when:
      label: "feature"
  - title: "🚀 Features"
    when:
      labels:
        - "feature"
        - "performance"
  - title: "🐛 Bug Fixes"
    when:
      label: "bug"
  - title: "📔 Documentation"
    when:
      label: "documentation"
  - title: "🏅 Code Quality Improvements"
    when:
      label: "code quality"
  - title: "🧪 Testing"
    when:
      label: "testing"
  - title: "🧱 Dependency Updates"
    when:
      label: "dependencies"
  - title: "🔧 Maintenance"
    when:
      labels:
        - "github structure"
        - "removal"
change-template: "- $TITLE @$AUTHOR (#$NUMBER)"
change-title-escapes: '\<*_&' # You can add # and @ to disable mentions, and add ` to disable code blocks.
version-resolver:
  default: patch
autolabeler:
  - label: "bug"
    title:
      - "/^fix[:(]/i"
  - label: "feature"
    title:
      - "/^feat[:(]/i"
  - label: "documentation"
    title:
      - "/^docs[:(]/i"
  - label: "testing"
    title:
      - "/^test[:(]/i"
  - label: "github structure"
    title:
      - "/^ci[:(]/i"
  - label: "code quality"
    title:
      - "/^refactor[:(]/i"
template: |
  ## Changes

  $CHANGES
