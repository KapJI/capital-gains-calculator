name: Publish Docker image

on:
  release:
    types: [published]
  push:
    branches:
      - main

permissions:
  contents: read

concurrency:
  group: docker-publish-${{ github.ref }}
  cancel-in-progress: true

env:
  IMAGE: ghcr.io/kapji/capital-gains-calculator

jobs:
  build-amd64:
    name: Build amd64 image
    if: github.repository_owner == 'KapJI'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    outputs:
      digest: ${{ steps.build.outputs.digest }}
    steps:
      - name: Check out the repository
        uses: actions/checkout@v7
        with:
          # Full history so git describe can compute the edge version.
          fetch-depth: 0

      - name: Compute version
        id: version
        run: |
          if [ "$GITHUB_EVENT_NAME" = "release" ]; then
            version="${{ github.event.release.tag_name }}"
          else
            # Edge builds are stamped relative to the latest release tag,
            # e.g. v2.0.0-107-gabc1234 -> 2.0.0.post107+gabc1234
            describe="$(git describe --tags --match 'v[0-9]*')"
            version="$(echo "${describe#v}" | sed -E 's/-([0-9]+)-g([0-9a-f]+)$/.post\1+g\2/')"
          fi
          echo "version=$version" >> "$GITHUB_OUTPUT"

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Docker metadata
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: ${{ env.IMAGE }}

      - name: Build and push by digest
        id: build
        uses: docker/build-push-action@v7
        with:
          context: .
          # Publish the runtime stage; the default test stage exists
          # for CI and includes uv.
          target: runtime
          platforms: linux/amd64
          build-args: |
            VERSION=${{ steps.version.outputs.version }}
          labels: ${{ steps.meta.outputs.labels }}
          provenance: false
          outputs: type=image,name=${{ env.IMAGE }},push-by-digest=true,name-canonical=true,push=true
          cache-from: type=gha,scope=publish-amd64
          cache-to: type=gha,mode=max,scope=publish-amd64

  build-arm64:
    name: Build arm64 image
    if: github.repository_owner == 'KapJI'
    runs-on: ubuntu-24.04-arm
    permissions:
      contents: read
      packages: write
    outputs:
      digest: ${{ steps.build.outputs.digest }}
    steps:
      - name: Check out the repository
        uses: actions/checkout@v7
        with:
          # Full history so git describe can compute the edge version.
          fetch-depth: 0

      - name: Compute version
        id: version
        run: |
          if [ "$GITHUB_EVENT_NAME" = "release" ]; then
            version="${{ github.event.release.tag_name }}"
          else
            # Edge builds are stamped relative to the latest release tag,
            # e.g. v2.0.0-107-gabc1234 -> 2.0.0.post107+gabc1234
            describe="$(git describe --tags --match 'v[0-9]*')"
            version="$(echo "${describe#v}" | sed -E 's/-([0-9]+)-g([0-9a-f]+)$/.post\1+g\2/')"
          fi
          echo "version=$version" >> "$GITHUB_OUTPUT"

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Docker metadata
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: ${{ env.IMAGE }}

      - name: Build and push by digest
        id: build
        uses: docker/build-push-action@v7
        with:
          context: .
          target: runtime
          platforms: linux/arm64
          build-args: |
            VERSION=${{ steps.version.outputs.version }}
          labels: ${{ steps.meta.outputs.labels }}
          provenance: false
          outputs: type=image,name=${{ env.IMAGE }},push-by-digest=true,name-canonical=true,push=true
          cache-from: type=gha,scope=publish-arm64
          cache-to: type=gha,mode=max,scope=publish-arm64

  publish:
    name: Publish multi-arch manifest
    needs:
      - build-amd64
      - build-arm64
    runs-on: ubuntu-latest
    environment:
      name: ghcr
      url: https://github.com/KapJI/capital-gains-calculator/pkgs/container/capital-gains-calculator
    permissions:
      packages: write
    steps:
      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Docker metadata
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: ${{ env.IMAGE }}
          tags: |
            type=edge,branch=main
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}

      - name: Create multi-arch manifest
        run: |
          docker buildx imagetools create \
            $(jq -cr '.tags | map("--tag " + .) | join(" ")' <<< "$DOCKER_METADATA_OUTPUT_JSON") \
            "$IMAGE@${{ needs.build-amd64.outputs.digest }}" \
            "$IMAGE@${{ needs.build-arm64.outputs.digest }}"

      - name: Inspect manifest
        run: |
          docker buildx imagetools inspect "$IMAGE:$(jq -cr '.tags[0] | split(":")[1]' <<< "$DOCKER_METADATA_OUTPUT_JSON")"
